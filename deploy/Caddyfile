__SITE_ADDRESS__ {
	@compressible not path /stream.mp3
	encode @compressible zstd gzip

	header -Server

	request_body {
		max_size 1MB
	}

	reverse_proxy __UPSTREAM__ {
		flush_interval -1
		lb_try_duration 5s
		lb_try_interval 250ms
	}
}
